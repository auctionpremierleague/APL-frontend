function add(x, y) { 
    return x + y; 
 } 
   
 function subtract(x, y) { 
    return x - y; 
 } 

 const monthName = ['Jan', 'Feb', 'Mar', 'Apr', 'May', 'Jun', 'Jul', 'Aug', 'Sep', 'Oct', 'Nov', 'Dec'];

 /**
 * @param {Date} d The date
 */
 export const cricDate = (d) => {
    var myHour = d.getHours();
    var amPm = (myHour < 12) ? "AM" : "PM";
    if (myHour > 12) myHour -= 12;
    var tmp = monthName[d.getMonth()+1] + ' '  + ("0" + d.getDate()).slice(-2) + ' . ' + 
        ("0" + myHour).slice(-2) + ':' + ("0" +  d.getMinutes()).slice(-2) + ' ' + amPm;
    return tmp;
}

   
 // Adding the code below to allow importing 
 // the functions in other files 
//  export default { add, cricDate } 